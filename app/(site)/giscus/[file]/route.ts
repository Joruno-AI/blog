const themes = { light: ":root{color-scheme:light;--color-canvas-default:#fff;--color-fg-default:#555;--color-border-default:rgba(0,0,0,.1)}", dark: ":root{color-scheme:dark;--color-canvas-default:#050505;--color-fg-default:#b8b8b8;--color-border-default:rgba(255,255,255,.14)}" } as const;
export async function GET(_request: Request, { params }: { params: Promise<{ file: string }> }) { const file = (await params).file; return new Response(themes[file === "dark.css" ? "dark" : "light"], { headers: { "Content-Type": "text/css; charset=utf-8", "Cache-Control": "public, max-age=86400" } }); }
