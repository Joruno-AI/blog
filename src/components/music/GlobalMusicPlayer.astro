---
import AlbumStage from '~/components/music/AlbumStage.astro'
import Gramophone from '~/components/music/Gramophone.astro'
import SongList from '~/components/music/SongList.astro'
import localAlbumsData from '~/content/music/data.json'

const albumsData = localAlbumsData
const firstAlbum = albumsData[0]
---

<div id="global-music-player" transition:persist="music-player">
  <div
    class="music-page-wrapper hidden"
    id="music-page-wrapper"
    style={`--album-color: ${firstAlbum?.color ?? '#7d503d'}; --album-cover: url("${firstAlbum?.cover ?? ''}");`}
  >
    <div class="music-ambient" aria-hidden="true">
      <div class="music-ambient-cover"></div>
      <div class="music-ambient-light"></div>
      <div class="music-ambient-noise"></div>
    </div>

    <div class="music-player-container">
      <div class="listening-stage">
        <AlbumStage albums={albumsData} />

        <section class="gramophone-section" aria-label="唱片机">
          <div class="gramophone-label" aria-hidden="true">
            <span>WOOD DECK · ORBIT VIEW</span>
            <i></i>
          </div>
          <div
            id="record-drop-target"
            class="record-drop-target"
            aria-hidden="true"
          >
          </div>
          <Gramophone />
          <p class="gramophone-hint">拖动旋转唱机 · 点击唱臂播放或暂停</p>
        </section>
      </div>

      <section class="playback-hub" id="playback-hub" aria-label="音乐播放控制">
        <aside class="playback-queue" id="playlist-drawer" aria-hidden="true">
          <SongList />
        </aside>

        <div class="now-playing-dock" id="mobile-mini-player">
          <div class="mini-track">
            <span class="mini-track-eyebrow" id="mini-track-eyebrow"
              >等待落针</span
            >
            <span class="mini-track-title" id="mini-track-title"
              >请选择一张唱片</span
            >
            <span class="mini-track-time" id="mini-track-time">0:00 / 0:00</span
            >
          </div>

          <div
            class="mini-progress"
            id="mini-progress"
            role="progressbar"
            aria-label="播放进度"
            aria-valuemin="0"
            aria-valuemax="100"
            aria-valuenow="0"
          >
            <div class="mini-progress-fill" id="mini-progress-fill"></div>
          </div>

          <div class="mini-controls">
            <button
              id="mini-prev"
              class="mini-ctrl-btn"
              type="button"
              aria-label="上一首"
              disabled
            >
              <span class="i-ri-skip-back-fill"></span>
            </button>
            <button
              id="mini-play"
              class="mini-ctrl-btn mini-play-btn"
              type="button"
              aria-label="播放或暂停"
              disabled
            >
              <span class="i-ri-play-fill" id="mini-play-icon"></span>
            </button>
            <button
              id="mini-next"
              class="mini-ctrl-btn"
              type="button"
              aria-label="下一首"
              disabled
            >
              <span class="i-ri-skip-forward-fill"></span>
            </button>
          </div>

          <div class="dock-speed-options" role="group" aria-label="播放速度">
            <button class="dock-speed-btn" type="button" data-rate="0.5"
              >.5×</button
            >
            <button class="dock-speed-btn active" type="button" data-rate="1"
              >1×</button
            >
            <button class="dock-speed-btn" type="button" data-rate="1.5"
              >1.5×</button
            >
            <button class="dock-speed-btn" type="button" data-rate="2"
              >2×</button
            >
          </div>

          <button
            class="queue-toggle"
            id="playlist-trigger"
            type="button"
            aria-label="展开曲目"
            aria-expanded="false"
            aria-controls="playlist-drawer"
          >
            <span class="i-ri-play-list-2-line"></span>
            <span class="queue-toggle-label">曲目</span>
            <i id="playlist-trigger-count">0</i>
            <span class="queue-chevron i-ri-arrow-up-s-line"></span>
          </button>

          <button
            class="dock-collapse-toggle"
            id="player-collapse-trigger"
            type="button"
            aria-label="收起播放条"
          >
            <span class="i-ri-arrow-down-s-line"></span>
          </button>
        </div>

        <div class="compact-player-dock" aria-hidden="true">
          <span class="compact-track-title" id="compact-track-title"
            >请选择一张唱片</span
          >
          <button
            class="compact-play-btn"
            id="compact-play"
            type="button"
            aria-label="播放或暂停"
            disabled
          >
            <span class="i-ri-play-fill" id="compact-play-icon"></span>
          </button>
          <button
            class="compact-expand-btn"
            id="player-expand-trigger"
            type="button"
            aria-label="展开播放条"
          >
            <span class="i-ri-arrow-up-s-line"></span>
          </button>
        </div>
      </section>
    </div>
  </div>
</div>

<style>
  #global-music-player {
    position: fixed;
    z-index: 100;
    inset: 0;
    pointer-events: none;
  }
  .music-page-wrapper {
    position: fixed;
    top: 0;
    right: 0;
    bottom: 0;
    left: 0;
    overflow: hidden;
    color: #f7f2ea;
    background: var(--music-room-surface, #0c0d0f);
    pointer-events: auto;
    transition:
      opacity 220ms ease,
      transform 280ms cubic-bezier(0.22, 1, 0.36, 1);
  }
  .music-page-wrapper.hidden {
    opacity: 0;
    pointer-events: none;
    transform: translateY(1.25rem);
  }
  .music-player-container,
  .listening-stage {
    position: relative;
    width: 100%;
    height: 100%;
  }
  .music-player-container {
    /* `overflow: hidden` creates a programmatically scrollable container.
       Focusing the queue toggle could therefore scroll the whole listening
       stage upward. `clip` keeps the crop without creating that scrollport. */
    overflow: clip;
  }
  .listening-stage {
    z-index: 2;
    display: grid;
    grid-template-rows: minmax(12rem, 0.44fr) minmax(13rem, 0.56fr);
    grid-template-columns: minmax(0, 1fr);
    padding-top: 4rem;
    padding-bottom: 4.1rem;
  }
  .gramophone-section {
    position: relative;
    display: flex;
    min-width: 0;
    min-height: 0;
    align-items: center;
    justify-content: center;
    overflow: hidden;
    border-top: 1px solid rgb(255 255 255 / 5%);
  }
  .gramophone-label {
    position: absolute;
    top: clamp(0.85rem, 2vh, 1.5rem);
    right: clamp(1.5rem, 4vw, 4.25rem);
    z-index: 4;
    display: flex;
    align-items: center;
    gap: 0.75rem;
    color: rgba(247, 242, 234, 0.48);
    font-size: 0.62rem;
    font-weight: 700;
    letter-spacing: 0.2em;
    pointer-events: none;
  }
  .gramophone-label i {
    width: 2.5rem;
    height: 1px;
    background: rgba(247, 242, 234, 0.24);
  }
  .record-drop-target {
    position: absolute;
    top: 53%;
    left: 50%;
    z-index: 6;
    width: clamp(5rem, 9vw, 8.5rem);
    aspect-ratio: 1;
    border-radius: 50%;
    opacity: 0;
    pointer-events: none;
    transform: translate(-50%, -50%);
  }
  .gramophone-hint {
    position: absolute;
    right: 50%;
    bottom: 1rem;
    z-index: 4;
    margin: 0;
    color: rgba(247, 242, 234, 0.38);
    font-size: 0.64rem;
    letter-spacing: 0.05em;
    white-space: nowrap;
    pointer-events: none;
    transform: translateX(50%);
  }

  .playback-hub {
    position: absolute;
    right: 0;
    bottom: 0;
    left: 0;
    z-index: 90;
    width: 100%;
    padding-inline: clamp(0.75rem, 4vw, 4.5rem);
    background: transparent;
    border-top: 0;
    box-shadow: none;
    transition:
      background-color 680ms cubic-bezier(0.22, 1, 0.36, 1),
      border-color 420ms ease;
  }
  .playback-queue {
    position: absolute;
    right: max(0.75rem, calc((100vw - 70rem) / 2));
    bottom: 100%;
    left: max(0.75rem, calc((100vw - 70rem) / 2));
    height: min(57vh, 34rem);
    overflow: hidden;
    color: #f7f2ea;
    background: color-mix(
      in srgb,
      var(--album-color, #28303a) 28%,
      rgb(10 11 13 / 95%)
    );
    border: 1px solid
      color-mix(in srgb, var(--album-color, #6f7885) 28%, white 12%);
    border-radius: 1rem 1rem 0 0;
    box-shadow: 0 -1.5rem 5rem rgb(5 6 8 / 32%);
    backdrop-filter: blur(24px) saturate(1.08);
    opacity: 0;
    visibility: hidden;
    pointer-events: none;
    transform: translate3d(0, 0.8rem, 0) scale(0.992);
    transform-origin: bottom center;
    transition:
      transform 260ms cubic-bezier(0.22, 1, 0.36, 1),
      opacity 170ms ease,
      visibility 260ms ease;
    will-change: transform, opacity;
  }
  .playback-queue.active {
    opacity: 1;
    visibility: visible;
    pointer-events: auto;
    transform: translate3d(0, 0, 0) scale(1);
  }
  .now-playing-dock {
    position: relative;
    z-index: 2;
    display: grid;
    grid-template-columns:
      minmax(9rem, 1fr) minmax(8rem, 1.2fr) auto auto auto
      auto;
    min-height: 3.85rem;
    width: min(70rem, 100%);
    align-items: center;
    gap: 0.75rem;
    margin-inline: auto;
    padding: 0.52rem 0.6rem 0.52rem 0.9rem;
    color: #f7f2ea;
    background: transparent;
    border: 0;
    border-radius: 0;
    box-shadow: none;
    backdrop-filter: none;
    transition:
      transform 220ms cubic-bezier(0.23, 1, 0.32, 1),
      opacity 160ms ease,
      background-color 280ms ease,
      border-color 280ms ease,
      visibility 220ms ease;
    will-change: transform, opacity;
  }
  .mini-track {
    display: grid;
    min-width: 0;
    grid-template-columns: 1fr auto;
    gap: 0.08rem 0.55rem;
  }
  .mini-track-eyebrow {
    grid-column: 1/-1;
    color: color-mix(in srgb, var(--album-tint, #d6b57b) 76%, white);
    font-size: 0.54rem;
    font-weight: 700;
    letter-spacing: 0.12em;
  }
  .mini-track-title {
    overflow: hidden;
    font-size: 0.72rem;
    font-weight: 650;
    text-overflow: ellipsis;
    white-space: nowrap;
  }
  .mini-track-time {
    color: rgba(247, 242, 234, 0.42);
    font-size: 0.56rem;
    font-variant-numeric: tabular-nums;
  }
  .mini-progress {
    position: relative;
    height: 3px;
    overflow: hidden;
    background: rgba(255, 255, 255, 0.13);
    border-radius: 999px;
    cursor: pointer;
  }
  .mini-progress-fill {
    position: absolute;
    width: 0;
    background: color-mix(in srgb, var(--album-tint, #d6b57b) 78%, white);
    border-radius: inherit;
    inset: 0 auto 0 0;
  }
  .mini-controls,
  .dock-speed-options {
    display: flex;
    align-items: center;
  }
  .mini-controls {
    gap: 0.12rem;
  }
  .mini-ctrl-btn {
    display: grid;
    width: 2.15rem;
    height: 2.15rem;
    padding: 0;
    color: rgba(247, 242, 234, 0.67);
    background: transparent;
    border: 0;
    border-radius: 999px;
    cursor: pointer;
    place-items: center;
    transition:
      color 150ms ease,
      background-color 150ms ease,
      transform 150ms ease;
  }
  .mini-ctrl-btn:disabled {
    opacity: 0.28;
    cursor: default;
  }
  .mini-play-btn {
    color: #111214;
    background: #f3eee5;
  }
  .mini-play-btn:disabled {
    color: #111214;
    background: rgba(243, 238, 229, 0.5);
  }
  .dock-speed-options {
    gap: 0.1rem;
    padding: 0.15rem;
    background: rgba(255, 255, 255, 0.055);
    border: 1px solid rgba(255, 255, 255, 0.08);
    border-radius: 0.65rem;
  }
  .dock-speed-btn {
    min-width: 1.85rem;
    height: 1.75rem;
    padding: 0 0.28rem;
    color: rgba(247, 242, 234, 0.43);
    background: transparent;
    border: 0;
    border-radius: 0.48rem;
    font-size: 0.58rem;
    cursor: pointer;
    transition:
      color 150ms ease,
      background-color 150ms ease,
      transform 150ms ease;
  }
  .dock-speed-btn.active {
    color: #111214;
    background: #eee8de;
  }
  .queue-toggle {
    display: flex;
    min-height: 2.4rem;
    align-items: center;
    gap: 0.42rem;
    padding: 0.38rem 0.48rem 0.38rem 0.62rem;
    color: rgba(247, 242, 234, 0.76);
    background: rgba(255, 255, 255, 0.055);
    border: 1px solid rgba(255, 255, 255, 0.09);
    border-radius: 0.72rem;
    cursor: pointer;
    transition:
      color 150ms ease,
      background-color 150ms ease,
      border-color 150ms ease,
      transform 150ms ease;
  }
  .dock-collapse-toggle,
  .compact-expand-btn,
  .compact-play-btn {
    display: grid;
    width: 2.4rem;
    height: 2.4rem;
    padding: 0;
    color: rgba(247, 242, 234, 0.76);
    background: rgba(255, 255, 255, 0.055);
    border: 1px solid rgba(255, 255, 255, 0.09);
    border-radius: 0.72rem;
    cursor: pointer;
    place-items: center;
    transition:
      color 150ms ease,
      background-color 150ms ease,
      border-color 150ms ease,
      transform 150ms ease;
  }
  .compact-player-dock {
    position: absolute;
    bottom: 0;
    left: 50%;
    z-index: 3;
    display: grid;
    width: min(22rem, calc(100vw - 1.2rem));
    min-height: 3.35rem;
    grid-template-columns: minmax(0, 1fr) auto auto;
    align-items: center;
    gap: 0.45rem;
    padding: 0.42rem 0.48rem 0.42rem 0.85rem;
    color: #f7f2ea;
    background: transparent;
    border: 0;
    border-radius: 0;
    box-shadow: none;
    backdrop-filter: none;
    opacity: 0;
    visibility: hidden;
    pointer-events: none;
    transform: translate3d(-50%, 0.5rem, 0) scale(0.965);
    transition:
      transform 220ms cubic-bezier(0.23, 1, 0.32, 1),
      opacity 150ms ease,
      background-color 280ms ease,
      border-color 280ms ease,
      visibility 220ms ease;
    will-change: transform, opacity;
  }
  .compact-track-title {
    overflow: hidden;
    font-size: 0.72rem;
    font-weight: 650;
    text-overflow: ellipsis;
    white-space: nowrap;
  }
  .compact-play-btn {
    color: #111214;
    background: #f3eee5;
    border-color: transparent;
    border-radius: 999px;
  }
  .compact-play-btn:disabled {
    opacity: 0.42;
  }
  .playback-hub.is-collapsed .now-playing-dock {
    opacity: 0;
    visibility: hidden;
    pointer-events: none;
    transform: translate3d(0, 0.5rem, 0) scale(0.985);
  }
  .playback-hub.is-collapsed .compact-player-dock {
    opacity: 1;
    visibility: visible;
    pointer-events: auto;
    transform: translate3d(-50%, 0, 0) scale(1);
  }
  .queue-toggle-label {
    font-size: 0.66rem;
    font-weight: 650;
  }
  .queue-toggle i {
    display: grid;
    min-width: 1.25rem;
    height: 1.25rem;
    color: rgba(247, 242, 234, 0.56);
    background: rgba(255, 255, 255, 0.07);
    border-radius: 999px;
    font-size: 0.54rem;
    font-style: normal;
    place-items: center;
  }
  .queue-chevron {
    font-size: 0.86rem;
    transition: transform 210ms cubic-bezier(0.22, 1, 0.36, 1);
  }
  .queue-toggle[aria-expanded='true'] .queue-chevron {
    transform: rotate(180deg);
  }
  .queue-toggle[aria-expanded='true'] {
    color: #fffaf2;
    background: rgba(255, 255, 255, 0.1);
    border-color: rgba(255, 255, 255, 0.17);
  }
  @media (hover: hover) and (pointer: fine) {
    .mini-ctrl-btn:not(:disabled):hover,
    .dock-speed-btn:hover,
    .queue-toggle:hover,
    .dock-collapse-toggle:hover,
    .compact-expand-btn:hover {
      color: #fffaf2;
      background-color: rgba(255, 255, 255, 0.1);
    }
    .mini-play-btn:not(:disabled):hover {
      color: #111214;
      background: #fffaf2;
      transform: scale(1.035);
    }
  }
  .mini-ctrl-btn:not(:disabled):active,
  .dock-speed-btn:active,
  .queue-toggle:active,
  .dock-collapse-toggle:active,
  .compact-expand-btn:active,
  .compact-play-btn:active {
    transform: scale(0.97);
  }

  @media (max-width: 1024px) {
    .gramophone-hint {
      display: none;
    }
    .now-playing-dock {
      grid-template-columns:
        minmax(8rem, 1fr) minmax(6rem, 0.9fr)
        auto auto auto auto;
      gap: 0.5rem;
    }
  }
  @media (max-width: 820px) {
    .music-page-wrapper {
      top: 0;
    }
    .listening-stage {
      grid-template-rows: minmax(13rem, 0.52fr) minmax(12rem, 0.48fr);
      grid-template-columns: 1fr;
      padding-top: 3.75rem;
      padding-bottom: 6rem;
    }
    .gramophone-label {
      top: 0.8rem;
      right: 1rem;
    }
    .record-drop-target {
      top: 54%;
      left: 50%;
      width: 4rem;
    }
    .playback-hub {
      bottom: 0;
      width: 100%;
      padding-inline: 0.55rem;
    }
    .playback-queue {
      height: min(54vh, 32rem);
      border-radius: 1rem;
    }
    .now-playing-dock {
      grid-template-areas:
        'track controls queue collapse'
        'progress speed speed speed';
      grid-template-columns: minmax(0, 1fr) auto auto auto;
      gap: 0.42rem 0.55rem;
      min-height: 5.7rem;
      padding: 0.48rem 0.52rem 0.55rem 0.72rem;
      border-radius: 0;
    }
    .mini-track {
      grid-area: track;
    }
    .mini-progress {
      grid-area: progress;
    }
    .mini-controls {
      grid-area: controls;
    }
    .dock-speed-options {
      grid-area: speed;
      justify-self: end;
    }
    .queue-toggle {
      grid-area: queue;
    }
    .dock-collapse-toggle {
      grid-area: collapse;
    }
    .mini-track-time,
    .mini-track-eyebrow {
      display: none;
    }
    .mini-track {
      display: block;
    }
  }
  @media (max-width: 480px) {
    .listening-stage {
      grid-template-rows: minmax(19.5rem, 0.57fr) minmax(11rem, 0.43fr);
    }
    .gramophone-label {
      display: none;
    }
    .queue-toggle-label {
      display: none;
    }
    .queue-toggle {
      gap: 0.28rem;
      padding-inline: 0.48rem;
    }
    .dock-speed-btn {
      min-width: 1.7rem;
      padding-inline: 0.2rem;
    }
  }
  @media (prefers-reduced-motion: reduce) {
    .music-page-wrapper,
    .playback-queue,
    .now-playing-dock,
    .compact-player-dock,
    .queue-chevron,
    .mini-ctrl-btn,
    .dock-speed-btn,
    .queue-toggle,
    .dock-collapse-toggle,
    .compact-expand-btn,
    .compact-play-btn {
      transition-duration: 1ms !important;
    }
  }
</style>

<script>
  interface MiniSong {
    name: string
  }
  interface MusicPlayerState {
    isPlaying: boolean
    isInitialized: boolean
    currentSongs?: MiniSong[]
    currentSongIndex?: number
    currentAlbumId?: string | null
    currentPlaybackRate?: number
    currentVinylRotation?: number
    shellEventsBound?: boolean
    playerCollapsed?: boolean
  }
  declare global {
    interface Window {
      MusicPlayerState: MusicPlayerState
    }
  }

  if (!window.MusicPlayerState) {
    window.MusicPlayerState = {
      isPlaying: false,
      isInitialized: false,
      currentPlaybackRate: 1,
      currentVinylRotation: 0,
    }
  }

  function isMusicPage() {
    return /^\/music\/?$/.test(window.location.pathname)
  }
  function updatePlayerVisibility() {
    document
      .getElementById('music-page-wrapper')
      ?.classList.toggle('hidden', !isMusicPage())
  }
  function formatTime(seconds: number) {
    if (!Number.isFinite(seconds) || seconds < 0) return '0:00'
    return `${Math.floor(seconds / 60)}:${String(Math.floor(seconds % 60)).padStart(2, '0')}`
  }
  function setControlsEnabled(enabled: boolean) {
    document
      .querySelectorAll<HTMLButtonElement>(
        '#mini-prev, #mini-play, #mini-next, #compact-play'
      )
      .forEach((button) => {
        button.disabled = !enabled
      })
  }
  function updateMiniTrack() {
    const state = window.MusicPlayerState
    const song = state.currentSongs?.[state.currentSongIndex ?? 0]
    const title = document.getElementById('mini-track-title')
    const compactTitle = document.getElementById('compact-track-title')
    const eyebrow = document.getElementById('mini-track-eyebrow')
    if (title) title.textContent = song?.name ?? '请选择一张唱片'
    if (compactTitle) compactTitle.textContent = song?.name ?? '请选择一张唱片'
    if (eyebrow)
      eyebrow.textContent = song
        ? state.isPlaying
          ? '正在播放'
          : '已就绪'
        : '等待落针'
    setControlsEnabled(Boolean(song))
  }
  function updatePlayIcon(isPlaying: boolean) {
    const icon = document.getElementById('mini-play-icon')
    const compactIcon = document.getElementById('compact-play-icon')
    if (icon) icon.className = isPlaying ? 'i-ri-pause-fill' : 'i-ri-play-fill'
    if (compactIcon)
      compactIcon.className = isPlaying ? 'i-ri-pause-fill' : 'i-ri-play-fill'
  }
  function setPlaylistOpen(open: boolean) {
    const playerContainer = document.querySelector<HTMLElement>(
      '.music-player-container'
    )
    if (playerContainer) playerContainer.scrollTop = 0
    const drawer = document.getElementById('playlist-drawer')
    const trigger = document.getElementById('playlist-trigger')
    drawer?.classList.toggle('active', open)
    drawer?.setAttribute('aria-hidden', String(!open))
    trigger?.setAttribute('aria-expanded', String(open))
    trigger?.setAttribute('aria-label', open ? '收起曲目' : '展开曲目')
  }
  function setPlayerCollapsed(collapsed: boolean) {
    const hub = document.getElementById('playback-hub')
    const fullDock = document.getElementById('mobile-mini-player')
    const compactDock = hub?.querySelector<HTMLElement>('.compact-player-dock')
    window.MusicPlayerState.playerCollapsed = collapsed
    hub?.classList.toggle('is-collapsed', collapsed)
    fullDock?.setAttribute('aria-hidden', String(collapsed))
    compactDock?.setAttribute('aria-hidden', String(!collapsed))
    if (collapsed) setPlaylistOpen(false)
  }
  function syncSpeed(rate: number) {
    window.MusicPlayerState.currentPlaybackRate = rate
    document
      .querySelectorAll<HTMLButtonElement>('.dock-speed-btn')
      .forEach((button) => {
        button.classList.toggle('active', Number(button.dataset.rate) === rate)
      })
  }
  function bindGlobalMusicEvents() {
    if (window.MusicPlayerState.shellEventsBound) return
    window.MusicPlayerState.shellEventsBound = true
    window.addEventListener('album-selected', ((event: CustomEvent) => {
      const album = event.detail
      if (!album || !Array.isArray(album.songs)) return
      window.MusicPlayerState.currentAlbumId = album.id ?? null
      window.MusicPlayerState.currentSongs = album.songs.map(
        (song: { name: string }) => ({ name: song.name })
      )
      window.MusicPlayerState.currentSongIndex = 0
      window.MusicPlayerState.isPlaying = false
      const count = document.getElementById('playlist-trigger-count')
      if (count) count.textContent = String(album.songs.length)
      const fill = document.getElementById('mini-progress-fill')
      const progress = document.getElementById('mini-progress')
      const time = document.getElementById('mini-track-time')
      if (fill) fill.style.width = '0%'
      progress?.setAttribute('aria-valuenow', '0')
      if (time) time.textContent = '0:00 / 0:00'
      updateMiniTrack()
      updatePlayIcon(false)
    }) as EventListener)
    window.addEventListener('playback-state', ((event: CustomEvent) => {
      const { isPlaying, songIndex } = event.detail
      window.MusicPlayerState.isPlaying = Boolean(isPlaying)
      if (typeof songIndex === 'number')
        window.MusicPlayerState.currentSongIndex = songIndex
      updateMiniTrack()
      updatePlayIcon(Boolean(isPlaying))
    }) as EventListener)
    window.addEventListener('playback-progress', ((event: CustomEvent) => {
      const { currentTime, duration, songIndex } = event.detail
      if (typeof songIndex === 'number')
        window.MusicPlayerState.currentSongIndex = songIndex
      const percent =
        duration > 0 ? Math.min(100, (currentTime / duration) * 100) : 0
      const fill = document.getElementById('mini-progress-fill')
      const progress = document.getElementById('mini-progress')
      const time = document.getElementById('mini-track-time')
      if (fill) fill.style.width = `${percent}%`
      progress?.setAttribute('aria-valuenow', String(Math.round(percent)))
      if (time)
        time.textContent = `${formatTime(currentTime)} / ${formatTime(duration)}`
      updateMiniTrack()
    }) as EventListener)
    window.addEventListener('set-playback-rate', ((event: CustomEvent) =>
      syncSpeed(Number(event.detail) || 1)) as EventListener)
  }
  function initialiseMusicShell() {
    bindGlobalMusicEvents()
    updatePlayerVisibility()
    const root = document.getElementById('music-page-wrapper')
    if (!root || root.dataset.shellBound === 'true') return
    root.dataset.shellBound = 'true'
    document
      .getElementById('playlist-trigger')
      ?.addEventListener('click', () =>
        setPlaylistOpen(
          !document
            .getElementById('playlist-drawer')
            ?.classList.contains('active')
        )
      )
    document
      .getElementById('mini-play')
      ?.addEventListener('click', () =>
        window.dispatchEvent(new CustomEvent('toggle-play'))
      )
    document
      .getElementById('compact-play')
      ?.addEventListener('click', () =>
        window.dispatchEvent(new CustomEvent('toggle-play'))
      )
    document
      .getElementById('mini-prev')
      ?.addEventListener('click', () =>
        window.dispatchEvent(new CustomEvent('prev-song'))
      )
    document
      .getElementById('mini-next')
      ?.addEventListener('click', () =>
        window.dispatchEvent(new CustomEvent('next-song'))
      )
    document
      .getElementById('mini-progress')
      ?.addEventListener('click', (event) => {
        const bar = event.currentTarget as HTMLElement
        const rect = bar.getBoundingClientRect()
        window.dispatchEvent(
          new CustomEvent('seek-to', {
            detail: Math.max(
              0,
              Math.min(1, (event.clientX - rect.left) / rect.width)
            ),
          })
        )
      })
    document
      .getElementById('player-collapse-trigger')
      ?.addEventListener('click', () => setPlayerCollapsed(true))
    document
      .getElementById('player-expand-trigger')
      ?.addEventListener('click', () => setPlayerCollapsed(false))
    document
      .querySelectorAll<HTMLButtonElement>('.dock-speed-btn')
      .forEach((button) => {
        button.addEventListener('click', () => {
          const rate = Number(button.dataset.rate)
          if (!Number.isFinite(rate)) return
          syncSpeed(rate)
          window.dispatchEvent(
            new CustomEvent('set-playback-rate', { detail: rate })
          )
        })
      })
    document.addEventListener('click', (event) => {
      const target = event.target as Node
      const hub = document.getElementById('playback-hub')
      if (hub && !hub.contains(target)) setPlaylistOpen(false)
    })
    document.addEventListener('keydown', (event) => {
      if (event.key === 'Escape' && isMusicPage()) setPlaylistOpen(false)
    })
    syncSpeed(window.MusicPlayerState.currentPlaybackRate ?? 1)
    setPlayerCollapsed(Boolean(window.MusicPlayerState.playerCollapsed))
    updateMiniTrack()
    updatePlayIcon(window.MusicPlayerState.isPlaying)
  }
  document.addEventListener('astro:page-load', initialiseMusicShell)
  initialiseMusicShell()
</script>
