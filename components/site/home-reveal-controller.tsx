"use client";

export function ViewportRevealGuard({
  rootSelector,
  targetSelector,
}: {
  rootSelector: string;
  targetSelector: string;
}) {
  void rootSelector;
  void targetSelector;
  return null;
}

/** Paints every home section immediately. */
export function HomeRevealController() {
  return null;
}
